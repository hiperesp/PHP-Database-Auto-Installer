CREATE TABLE `phpDbAutoInstaller` (
         `id`           INT             NOT NULL
        ,`test1`        VARCHAR(255)    NOT NULL
        ,`test2`        VARCHAR(255)    NOT NULL
        ,PRIMARY KEY(id)
);
